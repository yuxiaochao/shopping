package ${package};

import org.springframework.stereotype.Component;
import com.base.common.BaseServiceImpl;
import com.${project}.entity.${entity};
import com.${project}.service.${service};

@Component
public class ${serviceImpl} extends BaseServiceImpl<${entity}> implements ${service} {

}